import {TextInput} from "grommet";
import React from "react";

export const ParameterInput = ({label, value, onChange}) => {
    return (
        <div>
            <p>{label}:</p>

        </div>
    );
};
